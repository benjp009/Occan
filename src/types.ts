export interface CompanyRow {
  id: string;
  name: string;
  description: string;
  website: string;
  email: string;
  phone: string;
  siret: string;
  revenue2023: string;
  categories: string; // comma-separated
  keywords: string;   // comma-separated
  referral: string;
  logo: string;
}