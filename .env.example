# ===========================================
# Strapi CMS Configuration (Recommended)
# ===========================================
# Set to "true" to use Strapi instead of Google Sheets/Notion
REACT_APP_USE_STRAPI=true

# Strapi API URL
REACT_APP_STRAPI_URL=http://localhost:1337

# Strapi API Token (optional, for authenticated requests)
# REACT_APP_STRAPI_TOKEN=your-strapi-api-token

# ===========================================
# Legacy: Google Sheets Data Source
# ===========================================
# Only used when REACT_APP_USE_STRAPI is not "true"
REACT_APP_SHEET_CSV_URL="your-google-sheets-csv-url"

# Admin Authentication (server-side only - do not use REACT_APP_ prefix in production)
ADMIN_PASSWORD="your-secure-admin-password"

# Notion API (server-side only - do not use REACT_APP_ prefix)
# Only used when REACT_APP_USE_STRAPI is not "true"
NOTION_API_KEY=your-notion-api-key
NOTION_BLOG_DATABASE_ID=your-notion-database-id

# Optional: Monthly selection sheet URL
# REACT_APP_MONTH_CHOICE_CSV_URL="your-monthly-selection-csv-url"
